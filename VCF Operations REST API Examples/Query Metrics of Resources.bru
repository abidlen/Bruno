meta {
  name: Query Metrics of Resources
  type: http
  seq: 5
}

post {
  url: https://{{ServerName}}/suite-api/api/resources/stats/query
  body: json
  auth: inherit
}

vars:pre-request {
  ~epoch: (new Date()).getTime()
}

vars:post-response {
  ~epoch: (new Date()).getTime()
}

script:pre-request {
  bru.setVar("epoch", (new Date()).getTime() );
}
